# Configuração local do Claude Code
.claude/

# Configuração local com a SENHA do banco — nunca versionar
config.json

# Log do servidor (gerado em runtime)
servidor.log

# Sistema operacional
Thumbs.db
Desktop.ini
.DS_Store

# Backups de dados são opcionais no Git — descomente para NÃO versioná-los
# (podem ficar grandes por causa dos PDFs embutidos em base64)
# backup-rh-*.json
